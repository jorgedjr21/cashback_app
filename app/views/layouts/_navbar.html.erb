<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="#">Lemoney Test</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item <%= 'active' if params[:controller] == 'home'%>">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <% if user_signed_in? %>
        <li class="nav-item <%= 'active' if params[:controller] == 'admin'%>">
          <a class="nav-link" href="<%= admin_path %>">Admin</a>
        </li>
      <% end %>
    </ul>

    <% if user_signed_in? %>
      <%= link_to 'Logout', destroy_user_session_path, class: 'btn btn-secondary my-2', method: :delete %> 
    <% else %>
      <a class="btn btn-secondary my-2" href="<%= new_user_session_path %>">Login</a>
    <% end %>
  </div>
</nav>